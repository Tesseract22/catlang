import "std";
proc main() {
    let x := 5;
    let ref := x.&;

    ref.* = 10;

    print_int(x);

    let ref_ref := ref.&;

    let y := 69;
    ref_ref.* = y.&;

    print_int(ref.*);

    let sum := ref.* + ref_ref.*.*;
    print_int(sum);
}
